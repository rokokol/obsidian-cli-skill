# The policy tests/t.sh reads; the keys are in templates/t.conf of
# https://github.com/rokokol/tests-skill
#
# check-obsi.sh is this repository's behaviour suite and sits at the root, not under
# tests/, so without this t.sh prove took it for code and took a fix's new checks away
# with the fix
tests     check-obsi.sh
